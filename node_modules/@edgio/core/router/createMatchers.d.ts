import { Conditionals, Features, Matches } from '../types';
import RouteCriteria from './RouteCriteria';
/**
 * Creates the matching conditions for EdgeControl's `if` property.
 * @param criteria
 * @returns
 */
export declare function createMatchers(criteria: RouteCriteria): (Matches | Features | Conditionals)[];
