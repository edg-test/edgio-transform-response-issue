/// <reference types="node" />
export default class EdgeFunctionParentInterface {
    private started;
    private port;
    private workerData?;
    startListening(): void;
    fetchAsync(requestId: number, ...args: any[]): void;
    writeToStdout(buffer: Buffer): void;
    writeToStderr(buffer: Buffer): void;
    writeToDebugStream(buffer: Buffer): void;
    waitNextReadyResponseSync(): any;
    get path(): string;
}
