export default class EdgeFunctionMetrics {
    private activeTimers;
    private nextActiveTimerId;
    private values;
    private FIRST_INDEX;
    private LAST_INDEX;
    private microseconds;
    isValidIndex(index: number): boolean;
    isActiveIndex(index: number): boolean;
    startTimer(index: number): boolean;
    stopTimer(index: number): boolean;
    add(index: number, value: number): void;
    write(log: (arg: string) => any): void;
}
